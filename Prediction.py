# -*- coding: utf-8 -*-
"""Untitled38.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1xufI9Wdn7tR4iuDvpdUjGjqnezU425cw
"""

import pandas as pd
import pickle

class ModelPredictor:
    def __init__(self, model_path):
        with open(model_path, 'rb') as file:
            self.model = pickle.load(file)

    def preprocess_input(self, input_data):
        df = pd.DataFrame([input_data])
        return df

    def predict(self, input_data):
        preprocessed_data = self.preprocess_input(input_data)
        prediction = self.model.predict(preprocessed_data)
        return prediction

if __name__ == "__main__":
    predictor = ModelPredictor('/content/best_data (1) (1).pkl')
    new_input = {
        'CreditScore': 650, 'Age': 34, 'Tenure': 5, 'Balance': 50000,
        'NumOfProducts': 2, 'HasCrCard': 1, 'IsActiveMember': 1,
        'EstimatedSalary': 60000, 'Geography': 'France', 'Gender': 'Male'
    }
    result = predictor.predict(new_input)
    print("Hasil Prediksi:", result)

